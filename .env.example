# OpenClaw calls Ollama through the macOS host from inside Docker.
OLLAMA_BASE_URL=http://host.docker.internal:11434

# Persistent host path for OpenClaw data.
# Default target path for setup: ~/openclaw/data
OPENCLAW_DATA_DIR=~/openclaw/data
